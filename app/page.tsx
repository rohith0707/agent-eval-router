"use client";
import { useState } from "react";

const models = [
  { name: "GPT-5", quality: "91.8%", latency: "1.42s", cost: "$0.014", reliability: "96.2%" },
  { name: "Claude Sonnet", quality: "95.2%", latency: "1.88s", cost: "$0.021", reliability: "97.1%" },
  { name: "Local Llama", quality: "87.4%", latency: "0.76s", cost: "$0.002", reliability: "91.4%" },
];

export default function Home() {
  const [task, setTask] = useState(""); const [running, setRunning] = useState(false); const [result, setResult] = useState<any>(null);
  async function runEvaluation() { setRunning(true); try { const r = await fetch("/api/evaluate", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ task }) }); setResult(await r.json()); } finally { setRunning(false); } }
  return <div className="min-h-screen bg-[#09090b] text-zinc-100"><aside className="fixed inset-y-0 left-0 hidden w-60 border-r border-white/10 bg-[#0c0c0f] p-5 md:block"><div className="mb-8 flex items-center gap-3"><div className="grid h-8 w-8 place-items-center rounded-lg bg-white font-bold text-black">A</div><div><div className="font-semibold">Agent Eval</div><div className="text-xs text-zinc-500">Router</div></div></div><nav className="space-y-1">{["Overview","Evaluations","Models","Routing","Failures","Traces","Datasets","Reports"].map((x,i)=><div key={x} className={`rounded-lg px-3 py-2 text-sm ${i===0?"bg-white/10":"text-zinc-500 hover:bg-white/5"}`}>{x}</div>)}</nav></aside><main className="md:ml-60"><header className="flex h-16 items-center justify-between border-b border-white/10 px-6"><div><div className="text-xs text-zinc-500">Workspace / Agent Eval Router</div><h1 className="font-semibold">Evaluation Overview</h1></div><button onClick={runEvaluation} className="rounded-lg bg-white px-4 py-2 text-sm font-medium text-black">Run evaluation</button></header><div className="mx-auto max-w-7xl space-y-6 p-6 lg:p-8"><section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Quality","94.2%","+2.8%"],["p95 latency","1.62s","−14%"],["Cost / task","$0.016","−18%"],["Reliability","96.8%","+3.1%"]].map(([a,b,c])=><div key={a} className="rounded-2xl border border-white/10 bg-white/[.025] p-5"><div className="text-sm text-zinc-500">{a}</div><div className="mt-2 text-2xl font-semibold">{b}</div><div className="mt-2 text-xs text-emerald-400">{c} vs previous</div></div>)}</section><section className="rounded-2xl border border-white/10 bg-white/[.025] p-5"><h2 className="font-semibold">Routing Playground</h2><p className="text-sm text-zinc-500">Submit a task and inspect the routing decision.</p><div className="mt-4 flex flex-col gap-3 sm:flex-row"><input value={task} onChange={e=>setTask(e.target.value)} placeholder="e.g. Compare two approaches and return structured JSON" className="flex-1 rounded-lg border border-white/10 bg-black/20 px-4 py-3 text-sm outline-none"/><button disabled={running} onClick={runEvaluation} className="rounded-lg bg-white px-5 py-3 text-sm font-medium text-black disabled:opacity-50">{running?"Evaluating…":"Evaluate task"}</button></div>{result&&<div className="mt-5 rounded-xl border border-white/10 bg-black/20 p-4"><div className="grid gap-4 sm:grid-cols-4">{[["Selected",result.decision.selectedModel],["Quality",`${(result.metrics.quality*100).toFixed(1)}%`],["Latency",`${result.metrics.latencyMs}ms`],["Cost",`$${result.metrics.cost.toFixed(3)}`]].map(([a,b])=><div key={a}><div className="text-xs text-zinc-500">{a}</div><div className="mt-1 font-semibold">{b}</div></div>)}</div><div className="mt-4 text-sm text-zinc-400">{result.decision.reason}</div></div>}</section><section className="rounded-2xl border border-white/10 bg-white/[.025] p-5"><div className="mb-5 flex items-center justify-between"><div><h2 className="font-semibold">Model scorecard</h2><p className="text-sm text-zinc-500">Current benchmark baseline</p></div><span className="text-xs text-zinc-500">v0.1</span></div><table className="w-full text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase text-zinc-500"><tr><th className="py-3">Model</th><th>Quality</th><th>p95</th><th>Cost</th><th>Reliability</th></tr></thead><tbody>{models.map(m=><tr key={m.name} className="border-b border-white/5"><td className="py-4 font-medium">{m.name}</td><td>{m.quality}</td><td>{m.latency}</td><td>{m.cost}</td><td>{m.reliability}</td></tr>)}</tbody></table></section><section className="grid gap-6 lg:grid-cols-2"><div className="rounded-2xl border border-white/10 bg-white/[.025] p-5"><h2 className="font-semibold">Failure analysis</h2><div className="mt-5 space-y-3">{[["Retrieval failure",4],["Invalid output",3],["Tool failure",2],["Timeout",1]].map(([n,v])=><div key={n} className="flex justify-between rounded-lg border border-white/5 px-3 py-3 text-sm"><span>{n}</span><span className="text-zinc-500">{v} cases</span></div>)}</div></div><div className="rounded-2xl border border-white/10 bg-white/[.025] p-5"><h2 className="font-semibold">Routing policy</h2><div className="mt-5 space-y-3 text-sm">{[["Quality threshold","≥ 90%"],["Latency budget","≤ 2.5s"],["Cost ceiling","≤ $0.030"],["Fallback","Best quality"]].map(([a,b])=><div key={a} className="flex justify-between rounded-lg border border-white/5 px-3 py-3"><span>{a}</span><span className="text-zinc-400">{b}</span></div>)}</div></div></section></div></main></div>;
}
