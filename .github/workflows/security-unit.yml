name: Security Unit

on:
  push:
  pull_request:

jobs:
  input-security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: node scripts/test-input-security.mjs
